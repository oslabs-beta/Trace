import React from 'react';
import { render, screen } from '@testing-library/react';
// import chakra ui

describe('graphContainer')

it('Renders a Grid component')

it('Renders the "graphs" array')

describe('makeGraphs')

it('creates an array of GridItem components')

describe('flexComponent')

it('renders a flex component that has two immediate children')

















